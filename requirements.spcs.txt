# Requirements for SPCS (Snowflake Container Services) deployment.
# Omits Azure Blob dependencies — SPCS uses IMPACT_DATA_SOURCE=SQL or
# IMPACT_DATA_STORE=SNOWFLAKE, so azure-storage-blob is never needed.

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Geospatial processing
geopandas>=0.13.0
shapely>=2.0.0
pyproj>=3.4.0
matplotlib>=3.7.0
contextily>=1.4.0

# For Snowflake
snowflake-connector-python[pandas]>=3.0.0

# For Dash web app
dash>=2.14.0
plotly>=5.15.0
dash-mantine-components>=0.12.0
dash-iconify>=0.1.2
dash-leaflet>=0.2.0
dash-extensions>=1.0.0

# Production web server
gunicorn>=21.2.0
flask-compress>=1.14
brotli>=1.0.9

# Environment management
python-dotenv>=1.0.0

# GigaSpatial platform integration
# [all] required because giga-spatial unconditionally imports GEE handlers at
# package level regardless of which extras are installed.
giga-spatial[all]>=0.9.2
